# 使用方式

## 匯出一段對話

1. 在 Chrome 開啟要保存的 ChatGPT 或 `grok.com` 對話，等內容載入完成。
2. 點工具列上的 `GPT&Grok Exporter Local`。
3. 按「擷取目前對話」。長對話期間頁面會自動捲動，完成後會回到原本位置。
4. 查看「擷取品質」：
   - `未偵測到警告`：已確認從對話頂端掃到底部，且沒有偵測到明顯缺漏；仍不是逐字保證。
   - `請檢查警告`：可能缺回合、角色不完整，或無法確認已掃到最底部。
5. 視需要修改標題、語言與標籤。
6. 按「下載 .md」。檔案會進入 Chrome 的預設下載位置。

## 隱私判斷

這個工具不會把內容送出電腦，但匯出的 Markdown 本身仍可能含敏感資料。下載、同步或交給其他 AI 前，請自行檢查：

- 密碼、API key、token、cookie 或私密連結
- 病人或研究受試者的可識別資料
- 未公開的工作內容、姓名、地址、電話或帳號
- 不應進入雲端同步資料夾的附件文字

## 品質警告怎麼處理

- **沒有 Human 或 AI 回合**：通常是頁面尚未載完，或平台改了網頁結構。重新整理頁面後再試。
- **可能缺少回合**：先回原頁查看中段是否尚未載入，再重新擷取。
- **未確認捲動到底部**：極長對話可能沒有完整掃描；可先手動捲到最底，再重新擷取。
- **程式碼圍欄不成對**：原始訊息可能本來就含特殊反引號；下載後檢查相關 code block。

品質檢查是保守提示，不代表檔案一定錯誤；最終仍以原始 ChatGPT 對話為準。

## 要把匯出檔公開前

本工具的「local-only」只表示擷取過程不會上傳；它不會替你刪除對話原本含有的私人資料。公開 Markdown、範例或截圖前，請另外檢查姓名、email、帳號、檔案路徑、私人網址、病人資料、內部專案名稱與識別碼。

## 移除

在 `chrome://extensions/` 找到 `GPT&Grok Exporter Local`，按「移除」即可。工具沒有本機資料庫或雲端帳號需要另外刪除。
